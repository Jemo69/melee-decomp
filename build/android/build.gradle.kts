// Top-level build file: declares the plugin versions used by every module.
// The plugins are applied in the module-level build files (app/build.gradle.kts)
// without versions, so the versions are managed here in one place.
plugins {
    id("com.android.application") version "8.7.2" apply false
    id("com.chaquo.python") version "17.0.0" apply false
}
