org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.nonTransitiveRClass=true
# NOTE: Chaquopy 17 and AGP 8.x require running Gradle with JDK 17.
# Set JAVA_HOME to a JDK 17 install (or org.gradle.java.home to its path) before building.
